@page
@model GroupCCP.Pages.ComplaintFollowUps.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ComplaintFollowUp[0].Logs)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ComplaintFollowUp[0].Staff)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ComplaintFollowUp[0].FollowUp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ComplaintFollowUp[0].FollowUps)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ComplaintFollowUp) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Logs.LogId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Staff.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FollowUp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FollowUps.FollowUpId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.FollowUpId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.FollowUpId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.FollowUpId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
